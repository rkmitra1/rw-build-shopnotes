import type { Meta, StoryObj } from '@storybook/react'

import ShopNoteLayout from './ShopNotesLayout'

const meta: Meta<typeof ShopNoteLayout> = {
  component: ShopNoteLayout,
}

export default meta

type Story = StoryObj<typeof ShopNoteLayout>

export const Primary: Story = {}
